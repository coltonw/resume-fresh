import { FunctionalComponent } from "preact";
const SubHeading: FunctionalComponent = ({ children }) => (
  <div class="mt-8 mb-4 block justify-between lg:flex">{children}</div>
);

export default SubHeading;
