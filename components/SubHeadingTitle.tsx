import { FunctionalComponent } from "preact";
const SubHeadingTitle: FunctionalComponent = ({ children }) => (
  <h2 class="text-l font-black text-warmGray-800">{children}</h2>
);

export default SubHeadingTitle;
